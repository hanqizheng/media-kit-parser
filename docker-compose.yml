services:
  mysql:
    image: mysql:8.0
    container_name: general-agent-mysql
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: agent
    ports:
      - "3366:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
